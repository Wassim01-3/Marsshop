services:
  - type: web
    name: mars-shop-backend
    env: php
    rootDir: mars-shop-backend
    buildCommand: composer install --no-dev --optimize-autoloader
    startCommand: php -S 0.0.0.0:10000 -t public
